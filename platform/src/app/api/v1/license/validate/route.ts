/**
 * Versioned alias of /api/license/validate.
 *
 * This is the path baked into generated plugins
 * (LICENSE_SERVER + "/license/validate"). It is frozen — see ../README.md.
 */
export { POST } from "@/app/api/license/validate/route";
